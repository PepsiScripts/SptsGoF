Да, конечно, можно добавить две дополнительные кнопки слева, которые будут запускать и останавливать функцию `JumpFarm`. Вот обновленный скрипт:  ```lua local player = game.Players.LocalPlayer local playerGui = player:WaitForChild("PlayerGui")  local screenGui = Instance.new("ScreenGui") screenGui.Parent = playerGui  local isSpeedRunning = false local isJumpRunning = false  local speedTaskRef = nil local jumpTaskRef = nil  -- Создаем кнопку для запуска SpeedFarm local startSpeedButton = Instance.new("TextButton") startSpeedButton.Size = UDim2.new(0, 200, 0, 50) startSpeedButton.Position = UDim2.new(0.5, -100, 0.5, -75) startSpeedButton.Text = "Start SpeedFarm" startSpeedButton.Parent = screenGui  -- Создаем кнопку для остановки SpeedFarm local stopSpeedButton = Instance.new("TextButton") stopSpeedButton.Size = UDim2.new(0, 200, 0, 50) stopSpeedButton.Position = UDim2.new(0.5, -100, 0.5, 25) stopSpeedButton.Text = "Stop SpeedFarm" stopSpeedButton.Parent = screenGui  -- Создаем кнопку для запуска JumpFarm local startJumpButton = Instance.new("TextButton") startJumpButton.Size = UDim2.new(0, 200, 0, 50) startJumpButton.Position = UDim2.new(0.25, -100, 0.5, -75) startJumpButton.Text = "Start JumpFarm" startJumpButton.Parent = screenGui  -- Создаем кнопку для остановки JumpFarm local stopJumpButton = Instance.new("TextButton") stopJumpButton.Size = UDim2.new(0, 200, 0, 50) stopJumpButton.Position = UDim2.new(0.25, -100, 0.5, 25) stopJumpButton.Text = "Stop JumpFarm" stopJumpButton.Parent = screenGui  startSpeedButton.MouseButton1Click:Connect(function()     if not isSpeedRunning then         isSpeedRunning = true         speedTaskRef = task.spawn(function()             while isSpeedRunning do                 local args = {                     [1] = "SpeedFarm"                 }                 game:GetService("ReplicatedStorage").StatsFarmRemotes.StatFarm:FireServer(unpack(args))                 task.wait()             end         end)     end end)  stopSpeedButton.MouseButton1Click:Connect(function()     if isSpeedRunning then         isSpeedRunning = false     end end)  startJumpButton.MouseButton1Click:Connect(function()     if not isJumpRunning then         isJumpRunning = true         jumpTaskRef = task.spawn(function()             while isJumpRunning do                 local args = {                     [1] = "JumpFarm"                 }                 game:GetService("ReplicatedStorage").StatsFarmRemotes.StatFarm:FireServer(unpack(args))                 task.wait()             end         end)     end end)  stopJumpButton.Mous
